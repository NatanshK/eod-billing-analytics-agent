{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "comment": "Client-side routing: /analytics and /narrative are React routes, not files, so a hard refresh on them must still serve the app shell.",
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
